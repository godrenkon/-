import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.suiram.rpgedit',
  appName: 'Suiram RPG Edit',
  webDir: 'dist',
  android: { allowMixedContent: false },
};

export default config;
