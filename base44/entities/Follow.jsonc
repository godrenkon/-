{
  "name": "Follow",
  "type": "object",
  "properties": {
    "following_id": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "following_id"
  ],
  "rls": {
    "read": {},
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "created_by_id": "{{user.id}}"
    },
    "delete": {
      "created_by_id": "{{user.id}}"
    }
  }
}